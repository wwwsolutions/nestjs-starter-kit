version: '2.4'
services:
  postgres:
    ports:
      - ${POSTGRES_PORT}:5432

  pgadmin:
    ports:
      - ${PGADMIN_LISTEN_PORT}:5050

  redis:
    ports:
      - ${REDIS_PORT}:6379

  mongodb:
    - 27017:27017
