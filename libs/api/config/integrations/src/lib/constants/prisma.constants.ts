export enum PrismaLogLevel {
  ERROR = 'error',
  WARN = 'warn',
  INFO = 'info',
  QUERY = 'query',
}

export enum PrismaProvider {
  SQLITE = 'sqlite',
  POSTGRESQL = 'postgresql',
  MYSQL = 'mysql',
  SQLSERVER = 'sqlserver',
  MONGODB = 'mongodb',
  COCKROACHDB = 'cockroachdb',
}
